import { Link } from "react-router-dom";
import { NavLink } from "react-router-dom";
import { useAuth } from "../context/use-auth";
function AccountInfo({stock}) {
  let { user } = useAuth()
  return (
    <>
    
    </>
  );
}

export default CompanyInfo;
